//1) MatchedRule element deletion:
@action delete
@feature elements
@type Module
pattern mutation_operator_1
rule:ModuleElement from: ModuleElement.all.select(e|e.isTypeOf(MatchedRule)),
module:Module
{
	do {
		var instanceModule = rule.module;
		instanceModule.elements.remove(rule);
	}
}
