-- Unit [ LibraryRef [ 0, -1 ] libraries, add]

@action add
@libraries
pattern Unit_LibraryRef_libraries_add
instance:Unit
guard: instance.isTypeOf(ATL!Module) and instance.name = "Book2Publication"
{
	do{
		instance.println();
	}
}
