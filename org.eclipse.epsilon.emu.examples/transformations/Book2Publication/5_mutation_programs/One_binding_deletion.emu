@action delete
@feature bindings
pattern bindings_deletion
instance:SimpleOutPatternElement
guard: instance.type.name = "Female"
{
	do {
		var bind = instance.bindings.random();
		instance.bindings.remove(bind);
	}
}