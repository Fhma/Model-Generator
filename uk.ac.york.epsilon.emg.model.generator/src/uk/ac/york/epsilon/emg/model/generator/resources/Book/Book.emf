@namespace(uri="Book", prefix="")
package Book;

class Book {
  !ordered attr String[1] title;
  val Chapter[*]#book chapters;
}

class Chapter {
  !ordered attr String[1] title;
  !ordered attr int[1] nbPages;
  !ordered attr String[1] author;
  !ordered ref Book[1]#chapters book;
}

